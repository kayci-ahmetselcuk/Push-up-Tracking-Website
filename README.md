For JavaScript

supabase.createClient(): Connects to the database with URL and Anon Key.
document.getElementById(): Grabs HTML elements in JS.
parseInt() & isNaN(): Validates that the input is a real positive number.
new Date().setHours(0,0,0,0): Gets today's midnight timestamp for daily tracking.
supabase.from().insert(): Saves a new push-up entry to the database.
supabase.from().select() & forEach(): Fetches all rows and calculates Today and Total scores.
addEventListener("click"): Listens for the Add button click.
